__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env
.env.local
# Any copy of it, however it was named. Five .env backups reached the
# public repository on 10 August because only the exact name was listed
# here and `git add -A` swept up the rest.
.env.*
!.env.example

# runtime state, never source
#
# Anchored, and not just `data/`: unanchored it also matched
# app/src/main/java/com/example/push_to_talk/data/ - the Android app's whole
# network and repository layer - and silently kept it out of every commit.
# The app in this repository did not compile because of this line.
/services/process-mining/data/
db.sqlite3
media/
staticfiles/
*.log

.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
backups/

# smoke-test artifact
process_map.svg

# local agent settings
.claude/
.claude/settings.local.json

# TLS certificates are generated per host, never committed
services/nginx/certs/
